<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Transaction History</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
        <link rel="icon" type="image/png" sizes="32x32" href="/bingo-logo.png">
    <style>
       * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            background-color: #f8f9fa;
            font-family: Arial, sans-serif;
        }
        .SubAdminTransaction-container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 1rem;
        }

        .SubAdminTransaction-user-info {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 1.5rem;
            padding: 1rem;
            background-color: white;
            border-radius: 0.35rem;
            box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.1);
        }

        .SubAdminTransaction-user-info h2 {
            margin-bottom: 0;
            color: #5a5c69;
            font-weight: 700;
        }

        .SubAdminTransaction-total-badge {
            background-color: #4e73df;
            color: white;
            font-size: 1rem;
            padding: 0.5rem 1rem;
            border-radius: 0.35rem;
            box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
        }

        .SubAdminTransaction-badge-icon {
            margin-right: 8px;
        }

        .SubAdminTransaction-card {
            border: none;
            border-radius: 0.35rem;
            box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.1);
            margin-bottom: 2rem;
        }

        .SubAdminTransaction-card-header {
            background-color: white;
            border-bottom: 1px solid #e3e6f0;
            padding: 1rem 1.35rem;
            border-radius: 0.35rem 0.35rem 0 0 !important;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .SubAdminTransaction-card-header h4 {
            font-weight: 700;
            color: #4e73df;
            margin-bottom: 0;
        }

        .SubAdminTransaction-table-responsive {
            border-radius: 0.35rem;
            overflow: hidden;
        }

        .SubAdminTransaction-table th {
            background-color: #f8f9fc;
            color: #5a5c69;
            font-weight: 700;
            border-top: 1px solid #e3e6f0;
        }

        .SubAdminTransaction-table td {
            vertical-align: middle;
        }

        .SubAdminTransaction-total-row {
            font-weight: bold;
            background-color: #f8f9fa;
        }

        .SubAdminTransaction-text-danger {
            color: #e74a3b !important;
        }

        .SubAdminTransaction-text-success {
            color: #1cc88a !important;
        }

        .SubAdminTransaction-table-striped tbody tr:nth-of-type(odd) {
            background-color: rgba(0, 0, 0, 0.02);
        }

        @media (max-width: 768px) {
            .SubAdminTransaction-user-info {
                flex-direction: column;
                align-items: flex-start;
                gap: 1rem;
            }

            .SubAdminTransaction-card-header {
                flex-direction: column;
                align-items: flex-start;
                gap: 1rem;
            }
        }

        @media (max-width: 576px) {
            .SubAdminTransaction-container {
                padding: 0.5rem;
            }

            .SubAdminTransaction-user-info,
            .SubAdminTransaction-card {
                padding: 0.75rem;
            }

            .SubAdminTransaction-user-info h2 {
                font-size: 1.25rem;
            }

            .SubAdminTransaction-total-badge {
                font-size: 0.875rem;
                padding: 0.375rem 0.75rem;
            }
        }
    </style>
</head>

<body class="SubAdminTransaction-body">
    <%- include('../Partials/SubAdminHeader') -%>

        <div class="SubAdminTransaction-container">
            <div class="SubAdminTransaction-user-info">
                <h2><i class="fas fa-user-circle SubAdminTransaction-badge-icon"></i>Transaction History for <%=
                        subadminName %>
                </h2>
                <div class="SubAdminTransaction-total-badge">
                    <i class="fas fa-wallet SubAdminTransaction-badge-icon"></i>Total Credited: <%=
                        totalCredited.toFixed(2) %>
                </div>
            </div>

            <div class="SubAdminTransaction-card">
                <div class="SubAdminTransaction-card-header">
                    <h4><i class="fas fa-list SubAdminTransaction-badge-icon"></i>All Transactions</h4>
                    <div>
                        <span class="badge bg-primary">
                            Total Records: <%= transactions.length %>
                        </span>
                    </div>
                </div>
                <div class="card-body">
                    <div class="SubAdminTransaction-table-responsive">
                        <table class="table SubAdminTransaction-table SubAdminTransaction-table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Date</th>
                                    <th>Credited To</th>
                                    <th>Amount</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% transactions.forEach((transaction, index)=> { %>
                                    <tr>
                                        <td>
                                            <%= index + 1 %>
                                        </td>
                                        <td>
                                            <%= new Date(transaction.date).toLocaleString() %>
                                        </td>
                                        <td>
                                            <%= transaction.deposited_user_userName %>
                                        </td>
                                        <td class="SubAdminTransaction-text-danger">-<%=
                                                Math.abs(transaction.amount).toFixed(2) %>
                                        </td>
                                    </tr>
                                    <% }); %>
                                        <tr class="SubAdminTransaction-total-row">
                                            <td colspan="3">Total Credited Amount</td>
                                            <td class="SubAdminTransaction-text-success">
                                                <%= totalCredited.toFixed(2) %>
                                            </td>
                                        </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/js/all.min.js"></script>
</body>

</html>